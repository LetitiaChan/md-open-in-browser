{
  "name": "md-open-in-browser",
  "displayName": "Markdown / HTML Open in Browser",
  "description": "Open Markdown (.md/.mdc) and HTML (.html/.htm) files in your default browser with one right-click. Lightweight, zero-config, supports Windows/macOS/Linux.",
  "version": "1.1.2",
  "icon": "icon.png",
  "publisher": "letitia",
  "repository": {
    "type": "git",
    "url": "https://github.com/LetitiaChan/md-open-in-browser"
  },
  "engines": {
    "vscode": "^1.74.0"
  },
  "categories": [
    "Programming Languages",
    "Other"
  ],
  "keywords": [
    "markdown",
    "html",
    "browser",
    "preview",
    "open in browser",
    "md",
    "htm"
  ],
  "galleryBanner": {
    "color": "#1e1e1e",
    "theme": "dark"
  },
  "badges": [
    {
      "url": "https://img.shields.io/github/stars/LetitiaChan/md-open-in-browser?style=social",
      "href": "https://github.com/LetitiaChan/md-open-in-browser",
      "description": "GitHub Stars"
    }
  ],
  "activationEvents": [],
  "main": "./out/extension.js",
  "contributes": {
    "commands": [
      {
        "command": "md.openInBrowser",
        "title": "Open in Browser"
      }
    ],
    "menus": {
      "editor/context": [
        {
          "command": "md.openInBrowser",
          "when": "resourceExtname == .md || resourceExtname == .mdc || resourceExtname == .html || resourceExtname == .htm",
          "group": "navigation"
        }
      ],
      "editor/title/context": [
        {
          "command": "md.openInBrowser",
          "when": "resourceExtname == .md || resourceExtname == .mdc || resourceExtname == .html || resourceExtname == .htm",
          "group": "navigation"
        }
      ],
      "explorer/context": [
        {
          "command": "md.openInBrowser",
          "when": "resourceExtname == .md || resourceExtname == .mdc || resourceExtname == .html || resourceExtname == .htm",
          "group": "navigation"
        }
      ]
    }
  },
  "scripts": {
    "vscode:prepublish": "npm run compile",
    "compile": "tsc -p ./",
    "watch": "tsc -watch -p ./"
  },
  "devDependencies": {
    "@types/node": "^18.0.0",
    "@types/vscode": "^1.74.0",
    "sharp": "^0.34.5",
    "typescript": "^5.0.0"
  }
}
